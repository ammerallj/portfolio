# Archive — blob-hero landing (retired 2026-08-03)

Self-contained snapshot of the homepage as it was before the "Making products
make sense." landing (Figma 339:3745) replaced it.

- `index.html` — the blob-hero homepage (claim-first copy, cycling paragraphs
  ≤768, living blob with hover morph). Asset paths point at `../../images/`
  etc., and it loads its OWN copies of the styles/scripts below, so it keeps
  rendering even as the live site's shared files evolve.
- `style.css` + `css/` + `js/` — the shared styles and main.js exactly as of
  this date (blob BLOBS geometry, initHero morph/hover, initHeroCycle, the
  desktop/laptop/tablet/mobile hero tiers in responsive.css).

Open `index.html` directly (or via the dev server at
`/archive/blob-hero-2026-08/`) to view. Do not link from the live site.
